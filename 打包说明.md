# 语音转文字应用打包指南

## 方法一:使用打包脚本(推荐)

### 1. 安装PyInstaller
```bash
pip install pyinstaller
```

### 2. 运行打包脚本
```bash
python build_exe.py
```

### 3. 获取可执行文件
打包完成后,在 `dist` 目录下会生成 `语音转文字.exe`

---

## 方法二:手动使用PyInstaller命令

```bash
# 安装PyInstaller
pip install pyinstaller

# 打包命令
pyinstaller --name=语音转文字 ^
    --windowed ^
    --onefile ^
    --clean ^
    --add-data=".env.example;." ^
    --hidden-import=pyaudiowpatch ^
    --hidden-import=pyaudio ^
    --hidden-import=aiohttp ^
    --hidden-import=dotenv ^
    realtime_asr_client.py
```

---

## 打包参数说明

- `--windowed`: GUI模式,不显示黑色控制台窗口
- `--onefile`: 打包成单个exe文件(更方便分发)
- `--clean`: 清理之前的临时文件
- `--add-data`: 包含额外的数据文件
- `--hidden-import`: 显式导入PyInstaller可能检测不到的模块

---

## 使用打包后的程序

### 1. 配置API密钥
在exe文件同目录创建 `.env` 文件:
```
VOLCENGINE_APP_KEY=你的app_key
VOLCENGINE_ACCESS_KEY=你的access_key
VOLCENGINE_RESOURCE_ID=volc.bigasr.sauc.duration

# 实时翻译（可选，默认 DeepSeek V4.1 Flash）
TRANSLATE_API_KEY=你的deepseek_api_key
TRANSLATE_BASE_URL=https://api.deepseek.com
TRANSLATE_MODEL=deepseek-flash
```
翻译配置也可以在程序里点「翻译设置…」填写，保存后会自动写入这个 `.env`。

### 2. 运行程序
双击 `语音转文字.exe` 即可运行

### 3. 日志文件
程序会在exe同目录自动创建 `logs` 文件夹,保存运行日志

---

## 常见问题

### Q: 打包后运行报错"找不到pyaudio"
A: 确保在打包前已安装pyaudiowpatch:
```bash
pip install pyaudiowpatch
```

### Q: 打包后文件太大
A: 可以使用 `--onedir` 替代 `--onefile`,会生成文件夹但体积更小:
```bash
pyinstaller --name=语音转文字 --windowed --onedir realtime_asr_client.py
```

### Q: 想要添加图标
A: 使用 `--icon` 参数:
```bash
pyinstaller --icon=icon.ico ...其他参数
```

### Q: 打包后运行速度慢
A: 首次运行会解压文件,之后会快很多。使用 `--onedir` 模式会更快。

---

## 其他打包工具

### Nuitka(编译成C++,速度更快)
```bash
pip install nuitka
python -m nuitka --standalone --windows-disable-console ^
    --enable-plugin=tk-inter ^
    realtime_asr_client.py
```

### cx_Freeze
```bash
pip install cx_Freeze
cxfreeze realtime_asr_client.py --target-dir dist
```

---

## 分发建议

打包完成后,建议将以下文件一起分发:
- `语音转文字.exe` (主程序)
- `.env.example` (配置文件模板)
- `README.md` (使用说明)

用户只需:
1. 复制 `.env.example` 为 `.env`
2. 填入API密钥
3. 双击运行exe
